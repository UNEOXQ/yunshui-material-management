# 測試環境 - 啟用 GitHub 備份恢復功能
NODE_ENV=test
PORT=3004

# GitHub 備份配置（測試用）
GITHUB_TOKEN=test_token
GITHUB_OWNER=test_owner  
GITHUB_REPO=test_repo

# 其他配置
USE_MOCK_DATA=true
JWT_SECRET=test_jwt_secret_key_at_least_32_characters_long
JWT_EXPIRES_IN=24h
CORS_ORIGIN=http://localhost:3000
BCRYPT_ROUNDS=10